version: '3.8'

services:
  # Наш новый защитник и проводник
  caddy:
    image: caddy:latest
    restart: always
    ports:
      - "80:80"   # Обычный вход
      - "443:443" # Защищенный вход (HTTPS)
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - webapp

  db:
    image: postgres:15-alpine
    container_name: finance_bot_db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: finance_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  backend:
    build: ./backend
    container_name: finance_bot_backend
    restart: always
    depends_on:
      - db
    environment:
      DATABASE_URL: "postgresql://postgres:password@db:5432/finance_db?schema=public"
      PORT: 3000
      # 👇 ВСТАВЬТЕ СЮДА ВАШИ НАСТОЯЩИЕ КЛЮЧИ!
      TELEGRAM_BOT_TOKEN: "8552249372:AAFl1czLtrlzf0HhqbCggaTi62ldxvUn4qQ"
      OPENAI_API_KEY: "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"
      # 👇 ВАШ НОВЫЙ ДОМЕН (с https://)
      WEBAPP_URL: "https://loomybot.ru" 
    ports:
      - "3000:3000"
    command: sh -c "npx prisma db push && node src/server.js"

  webapp:
    build: ./webapp
    container_name: finance_bot_webapp
    restart: always
    # Порты убрали - теперь сайт доступен только через Caddy

volumes:
  postgres_data:
  caddy_data:
  caddy_config: